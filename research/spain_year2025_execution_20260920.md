# 西班牙2025年条件性全年回测执行记录

## 授权与口径

用户已确认：先执行2025年7天滚动，提交独立审核后执行整段联合。100 MW / 200 MWh独立储能，电量安全区10—190 MWh，单向效率92%，并网购售电各100 MW；完美信息、理想成交、系统平均激活代理、两种固定激活顺序等既定假设不变。毛收益，不扣未约定成本，不引入现实中标率或冲击系数。

经济可用性两项P1（成交及市场冲击、单站容量可交付性）仍保留；用户授权的是披露上述限制的条件性试算，不是正式数据/商业可用性放行。实际主线程档位不以配置文档声称运行时已切换；独立审核使用Astra medium。

## 已完成：年度覆盖验收

Madrid当地2025-01-01 00:00至2026-01-01 00:00，右端不含。

| 项目 | 数值 |
|---|---:|
| 全年目标 | 35,040个QH / 8,760小时 |
| 四情景共同有效 | 32,045个QH / 8,011.25小时 |
| 排除或缺失 | 2,995个QH / 748.75小时 |
| 覆盖率 | 91.4526% |
| 2025全年共享循环预算 | 548.76617033949次完整循环 |

共同掩码含合同边界闭包，不是只按单字段非空统计。原生ts_start覆盖32,112个QH，ts_end覆盖32,090个QH；两容量时间表在各自标签下覆盖相同。预算按已审本地日比例（含夏令时）折算，不简单将小时占比乘600，也不在每段重发。

验收输出：`outputs/es_historical_conditional/year2025_coverage_20260920_v1/summary.json`及四情景manifest/input；该次只验收数据，经济运行数为0。真正执行会绑定执行时的完整依赖哈希。

## 收益展示

先报告实际32,045个有效QH的累计毛收益及分项。若另给折算年化，使用“有效区间毛收益 × 8,760 / 8,011.25”，系数1.0934623186144485，并显著标注这是按有效小时平均收益外推，假定缺失时段与有效时段相似，并非完整全年测算或实际收益预测。缺失可能非随机，因此折算值只供参考。

## 受控执行顺序

1. 单情景入口和断点恢复测试通过后，首先运行ts_start/cap_old/U的rolling7代表组合。
2. 代表组合独立审核后，串行处理其余滚动情景/顺序；既有四情景及U/D标签不混合。
3. 滚动阶段独立审核后，启动整段联合；仍保持全年共享预算，不能为降低资源而改成各段独立优化。
4. 联合审计完成后形成两版对照；未证明最优的可行解和求解上界分别披露。滚动窗口gap不解释为全年全局gap。

资源：每窗求解限时30秒、相对gap目标1e-5；实际运行含准备、检查点、审计与序列化，单进程工作集3 GiB护栏，不等于private commit或整机内存限制。全年总耗时不能沿用资源演练600秒上限。中止时保留最后成功持久化的断点，不将部分现金标为全年收益。禁止覆盖原始数据和旧回测结果。

状态：覆盖验收完成；全年经济求解尚未开始，等待单情景执行入口及只读审核。官方依据沿用已审REE/eSIOS与OMIE本地资料，本次无新增市场规则或原始数据补齐。
