# 西班牙完整模型：合成开发与真实回测分开放行

日期：2026-09-20。用户明确回复“是的，分开放行”。

## 本次批准

依据已审核的 `model/es_perfect_information_design_20260920.md` draft-4，允许实现完整的日前、日内拍卖、aFRR容量和激活耦合模型，提供7天滚动与整段联合两版，各自固定U/D顺序。输入仅限显式构造的合成算例。

批准内容包括交易事件、跨窗固定订单、独立现金账本、SOC与年度循环约束、滚动末端估值和事后诊断。合成价格、事件时间和激活比例均为测试假设，不是新增西班牙规则事实。费用继续不纳入毛收益，不解释为实际不存在。

## 阶段验收

1. 输入契约与联合优化核心：手算、反例及回归测试，独立审核通过后进入下一阶段。
2. 滚动窗口、订单状态和现金账本：跨窗、跨年、缺口、DST及重复结算测试，再独立审核。
3. 集成、诊断及四组合测试：独立终审；未实现项明确登记，不能用局部通过替代完整通过。

工程角色负责代码；主线程负责授权、整合和验收；审核角色只读，不得委派下级。审核目标配置为Astra/medium。

## 继续关闭的权限

真实数据读取接口、真实历史收益回测和生产运行继续关闭。680/681单位、记录区间、分母总体可比性及全市场合同/规则/价格联合验收仍须完成。129个条件性候选区间不能直接进入收益计算。

保留 `implementation_approved=false` 作为真实历史实施未批准的旧字段；新增 `full_model_synthetic_implementation_approved=true` 明确本次范围。真实数据门禁不再阻塞合成代码开发，但仍阻塞真实回测。

本次是授权登记，不是实现完成或测试通过的证明。没有新增官方规则来源，沿用已审设计的来源与明确建模假设。
